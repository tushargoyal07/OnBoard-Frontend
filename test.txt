
const prirotity=[
    {name: 'low',id: 0},
    {name: 'high',id: 1}
]

function filter(id: string|number){
    const resp=data.filter((data)=>data.key===id)
    return resp
}

if e.prirotity===key:
    prirotity logic here

prirotity.map((index,value)=>{
    data=filter(id)
    data.map((index,value)=>{

    })
})


enum e{
    user="user"
    prirotity="prirotity"
}

setkey(e.prirotity)